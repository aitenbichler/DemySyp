apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontendkeycloakdemo
  namespace: student-h-aitenbichler
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontendkeycloakdemo
  template:
    metadata:
      labels:
        app: frontendkeycloakdemo
    spec:
      containers:
        - name: frontendkeycloakdemo
          image: docker.io/aiten/frontendkeycloakdemo:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              protocol: TCP
          resources:
            limits:
              cpu: "500m"
              memory: "256Mi"
            requests:
              cpu: "250m"
              memory: "128Mi"            
---
apiVersion: v1
kind: Service
metadata:
  name: frontendkeycloakdemo-svc
  namespace: student-h-aitenbichler
spec:
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: frontendkeycloakdemo
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontendkeycloakdemo-ingress
  namespace: student-h-aitenbichler
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
spec:
  ingressClassName: nginx
  rules:
    - host:  h-aitenbichler.cloud.htl-leonding.ac.at
      http:
        paths:
          - path: /frontendkeycloakdemo(/|$)(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: frontendkeycloakdemo-svc
                port:
                  number: 80
